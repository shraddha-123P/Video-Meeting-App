**Project Summary 💻👥:**

-Implemented DOM manipulation to dynamically update UI based on user actions (login, join meeting, logout)

-Managed UI state transitions (logged out → logged in → in meeting) using JavaScript

-Handled event-driven interactions such as button clicks for login, join, mute, camera, and logout

-Created and controlled dynamic elements (like logout button) using JavaScript

-Built a Zoom-like meeting UI simulation using HTML, CSS, and JavaScript
